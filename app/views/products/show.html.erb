<h1><%= @product.name%></h1>
<h5>$<%= @product.price%></h4>
<%= @product,image.attached? ? (image_tag @product.images.first, alt: 'Image of GM bat') : (image_tag 'GMBat.jpg', alt:'Default image of Bat') %>
 <p> 
 <div>
  <%= link_to "edit", edit_product_path(@product),class:"btn btn-success" %> 
    <div class="edit-box"> 
    </div>

</div>
  </p> 
  
  <%= link_to 'DESTROY', @product, method: :delete,
        data: { confirm: 'Are you sure?' }, class:'btn btn-danger m-1' %>



<% @product.ratings.each do |rating|%>
<h5><%= rating.user.name%></h5>
<p>Review: <%= rating.review%> </p>
<p>Rating: <%= rating.rating%>  </p>


<%end%>


